/**
 * Central inventory of `data-testid` attributes used by the Playwright E2E
 * suite.
 *
 * The canonical definitions live in `src/presentation/test-ids.ts` so the
 * frontend and tests stay in sync.
 */
export { testIds } from "../src/presentation/test-ids";
export type { TestIds } from "../src/presentation/test-ids";
