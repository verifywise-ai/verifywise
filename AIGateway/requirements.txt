# Core
fastapi>=0.141.1
uvicorn>=0.52.4
python-dotenv>=1.2.3
pydantic>=2.13.5
pydantic-settings>=2.15.0
redis>=8.1.0
cryptography>=50.0.1
httpx>=0.28.1

# Database (async PostgreSQL)
sqlalchemy[asyncio]>=2.0.52
asyncpg>=0.31.0

# LiteLLM SDK (MIT licensed)
# Pinned to a specific version — updated per VerifyWise release
litellm==1.98.0

# Database migrations (Alembic)
sqlalchemy[asyncio]>=2.0.52
asyncpg>=0.29.0
alembic>=1.19.1

# Guardrails — PII detection (in-process, no Docker)
presidio-analyzer>=2.2.364
# presidio-anonymizer 2.2.364 caps cryptography<49.0.0, but VerifyWise requires >=50.0.0.
# It is installed with --no-deps in CI/Docker since cryptography is the only required dep.
presidio-anonymizer>=2.2.364
# spaCy model: python -m spacy download en_core_web_md
spacy>=3.8.16

# MCP Gateway — Model Context Protocol SDK
mcp>=2.1.1

# Observability — OpenTelemetry metrics + logs (pushed to central OTLP collector)
opentelemetry-api>=1.44.0
opentelemetry-sdk>=1.44.0
opentelemetry-exporter-otlp-proto-http>=1.44.0
