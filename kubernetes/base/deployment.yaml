apiVersion: apps/v1
kind: Deployment
metadata:
  name: verifywise
spec:
  selector:
    matchLabels:
      app: verifywise
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: verifywise
    spec:
      automountServiceAccountToken: false
      # Pod-level security context (KSV-0118): runtime-default seccomp profile.
      # Container-level contexts below already drop capabilities and disable
      # privilege escalation; runAsNonRoot is a tracked TODO pending non-root
      # image USER directives (see per-container TODO comments).
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: verifywise-backend
          image: ghcr.io/verifywise-ai/verifywise-backend:1.7.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: web
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 200m
              memory: 768Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          # TODO: add `runAsNonRoot: true` once Servers/Dockerfile sets a
          # non-root USER (the node:24-alpine3.20 image currently runs as root).
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: verifywise-config
            - secretRef:
                name: verifywise-secret
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: BACKEND_PORT
            - name: DB_HOST
              value: localhost
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_USER
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_PORT
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: verifywise-secret
                  key: DB_PASSWORD
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_NAME
            - name: MOCK_DATA_ON
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: MOCK_DATA_ON
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: verifywise-secret
                  key: JWT_SECRET
            - name: SMTP_HOST
              value: smtp.gmail.com
            - name: SMTP_PORT
              value: "587"
            - name: LLM_EVALS_URL
              value: http://localhost:8000
            - name: AI_GATEWAY_URL
              value: http://localhost:8100

        - name: verifywise-worker
          image: ghcr.io/verifywise-ai/verifywise-backend:1.7.0
          imagePullPolicy: IfNotPresent
          command: ["node", "dist/jobs/worker.js"]
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 200m
              memory: 768Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: verifywise-config
            - secretRef:
                name: verifywise-secret
          env:
            - name: DB_HOST
              value: localhost
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_USER
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_PORT
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: verifywise-secret
                  key: DB_PASSWORD
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_NAME
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: verifywise-secret
                  key: JWT_SECRET
            - name: REDIS_URL
              value: redis://localhost:6379/0
            - name: AI_GATEWAY_URL
              value: http://localhost:8100

        - name: verifywise-frontend
          image: ghcr.io/verifywise-ai/verifywise-frontend:1.7.0
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          envFrom:
            - configMapRef:
                name: verifywise-config
            - secretRef:
                name: verifywise-secret
          ports:
            - containerPort: 80
              name: web
              protocol: TCP
          # TODO: switch Clients/Dockerfile to nginxinc/nginx-unprivileged (or a
          # non-root nginx variant) and then add `runAsNonRoot: true` here. The
          # current image (nginx:1.31.3-alpine) starts its master process as root.
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
              # nginx must bind port 80 as root, so keep NET_BIND_SERVICE.
              add:
                - NET_BIND_SERVICE

        - name: verifywise-redis
          image: redis:7
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 6379
              name: web
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 999
            capabilities:
              drop:
                - ALL

        - name: verifywise-ai-gateway
          image: ghcr.io/verifywise-ai/verifywise-ai-gateway:1.7.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8100
              name: web
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 200m
              memory: 768Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8100
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8100
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          # TODO: add `runAsNonRoot: true` once AIGateway/Dockerfile sets a
          # non-root USER (the python:3.12-slim image currently runs as root).
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: verifywise-config
            - secretRef:
                name: verifywise-secret
          env:
            - name: DB_HOST
              value: localhost
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_USER
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_PORT
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: verifywise-secret
                  key: DB_PASSWORD
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_NAME
            - name: REDIS_URL
              value: redis://localhost:6379/0
            - name: EXPRESS_BACKEND_URL
              value: http://localhost:3000
            - name: AI_GATEWAY_PORT
              value: "8100"

        - name: verifywise-eval-server
          image: ghcr.io/verifywise-ai/verifywise-eval-server:1.7.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
              name: web
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 200m
              memory: 768Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          # TODO: add `runAsNonRoot: true` once EvalServer/Dockerfile sets a
          # non-root USER (the python:3.12-slim image currently runs as root).
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: verifywise-config
            - secretRef:
                name: verifywise-secret
          env:
            - name: DB_HOST
              value: localhost
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_USER
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_PORT
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: verifywise-secret
                  key: DB_PASSWORD
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_NAME
            - name: BACKEND_URL
              value: http://localhost:3000
            - name: REDIS_URL
              value: redis://redis:6379/0
          volumeMounts:
            - name: eval-uploads
              mountPath: /app/data/uploads

        - name: verifywise-postgres-temp
          image: postgres:16.8
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
              name: web
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 200m
              memory: 512Mi
          securityContext:
            runAsNonRoot: true
            runAsUser: 999
          env:
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_NAME
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: verifywise-config
                  key: DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: verifywise-secret
                  key: DB_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
      volumes:
        - name: eval-uploads
          persistentVolumeClaim:
            claimName: eval-server-uploads
